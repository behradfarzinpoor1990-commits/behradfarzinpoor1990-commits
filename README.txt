نسخه کامل اولیه وبلاگ
1) index.html = صفحه وبلاگ
2) admin.html = پنل مدیریت
3) رمز پیش‌فرض پنل: 1234

نکته مهم:
این نسخه برای شروع و تست ساخته شده و اطلاعات را فقط در localStorage مرورگر ذخیره می‌کند.
برای استفاده واقعی روی اینترنت باید احراز هویت امن، بک‌اند، پایگاه‌داده و آپلود فایل به سرور اضافه شود.
